"""CLI entry points for the trading app.

All user-invokable scripts live here.  Run any of them with:

    python scripts/<name>.py [args]

from the project root.  Importers can also do:

    from scripts.signal_runner import run

because this folder is a proper package.
"""
